// Static app configuration (non-secret). Env-driven values come from lib/env.ts.
export const site = {
  name: "MANEXA",
  tagline: "AI-Powered School Management",
  supportEmail: "support@manexa.in",
  brand: { accent: "#B6FF2A", logo: "#BED740" },
} as const;
